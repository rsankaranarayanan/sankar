COMMON:
  cf_space_name: << Space Name >> # Cloud Foundry Space Name ex. sankar-test.
  cf_org_name: << Org Name >> # Cloud Foundry Org Name ex. SANARK-CFORG.
  cf_api_host: << cloud foundry api host >> # Cloud Foundry API host
  cf_login_host: << cloud foundry login host >> # Cloud Foundry Login Host.
  vault_proxy_url: << vault proxy url >> # Vault Proxy url (Endpoint details).
  space_vault_name: << vault service name >> # Vault Name which is used to store credentials.
